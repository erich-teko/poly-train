info:
  name: registre user
  type: http
  seq: 2

http:
  method: POST
  url: http://{{host}}:{{port}}/auth/register
  body:
    type: json
    data: |-
      {
        "username": "HansWurst",
        "email": "hans.wurst@xyz.abc",
        "password": "12345"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Protected route profile
  type: http
  seq: 5

http:
  method: GET
  url: http://{{host}}:{{port}}/profile
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: users Post
  type: http
  seq: 3

http:
  method: POST
  url: http://{{host}}:{{port}}/users
  body:
    type: json
    data: |
      {
        "username": "Erich",
        "email": "erich.imfeld@edu.teko.ch",
        "password": "12345"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

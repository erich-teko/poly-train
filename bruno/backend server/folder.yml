info:
  name: backend server
  type: folder
  seq: 2

request:
  auth:
    type: bearer
    token: "{{token}}"
  variables:
    - name: port
      value: "3100"
    - name: host
      value: localhost


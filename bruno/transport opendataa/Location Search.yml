info:
  name: Location Search
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/locations?query=paris"
  params:
    - name: query
      value: paris
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
